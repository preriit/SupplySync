/**
 * Inter — matches `frontend/public/DESIGN_SYSTEM.md` (loaded in `app/_layout.js`).
 * Use these families instead of `fontWeight` so Android renders the correct face.
 */
export const FONT = {
  regular: 'Inter_400Regular',
  medium: 'Inter_500Medium',
  semibold: 'Inter_600SemiBold',
  bold: 'Inter_700Bold',
};
